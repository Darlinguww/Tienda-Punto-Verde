services:
  app:
    build:
      context: .
      target: production
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DISABLE_HMR=false
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped
